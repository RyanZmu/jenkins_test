---
- hosts: rocky

  tasks:
   - name: Install node.js dependencies
     command:
      chdir: /usr/local/opt/node/book-lookup-app
      cmd: |
       npm install
       npm run build
       npm start